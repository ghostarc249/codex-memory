[features]
memories = true
codex_hooks = true

[mcp_servers.codex_memory]
command = "codex-memory"
args = ["mcp"]
enabled = true
required = false
startup_timeout_sec = 10
tool_timeout_sec = 30

# Repo-local automatic memory hooks are installed into:
# <repo>/.codex/hooks.json
